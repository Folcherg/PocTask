<UserControl x:Class="PocTask.TestPause"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PocTask"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="This">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ToggleButton Grid.Row="0" Margin="2" Checked="Pause" Unchecked="Resume">Pause</ToggleButton>
        <ListView Grid.Row="1" ItemsSource="{Binding ElementName=This, Path=List}"></ListView>
    </Grid>
</UserControl>
